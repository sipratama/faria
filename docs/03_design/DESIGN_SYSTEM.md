# Design System — FARIA

> **Status:** Not activated for V1.
>
> FARIA V1 uses the `prototype` base profile with the `ai-enabled` modifier (see `docs/PROJECT_INITIALIZATION.md`). A formal design system was intentionally left out of the lean V1 documentation baseline established during initialization — the V1 dashboard is a simple Agent Control Center, not a design-system-driven product surface.
>
> Activate this document once the dashboard grows enough reusable UI patterns/tokens to need a shared design reference.

## Related Documents

- `AGENTS.md`
- `docs/PROJECT_INITIALIZATION.md`
- `docs/02_architecture/SYSTEM_ARCHITECTURE.md`

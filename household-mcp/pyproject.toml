[project]
name = "faria-household-mcp"
version = "0.1.0"
description = "Constrained FARIA household-domain MCP server"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116,<1",
    "mcp>=2.2.0,<3",
    "uvicorn>=0.35,<1",
]

[project.scripts]
faria-household-mcp = "faria_household_mcp.server:main"
faria-dashboard-api = "faria_household_mcp.dashboard_api:main"

[dependency-groups]
dev = [
    "httpx>=0.28,<1",
    "pytest>=8.4,<9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/faria_household_mcp"]

[tool.hatch.build.targets.wheel.force-include]
"migrations" = "faria_household_mcp/migrations"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["--strict-config", "--strict-markers"]
